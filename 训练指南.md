# YOLOv8 训练指南

## 📚 目录

1. [快速开始](#快速开始)
2. [数据集准备](#数据集准备)
3. [训练方法](#训练方法)
4. [训练参数详解](#训练参数详解)
5. [训练示例](#训练示例)
6. [常见问题](#常见问题)

---

## 🚀 快速开始

### 方法一：命令行训练（CLI）

```bash
# 激活虚拟环境
cd /home/xxy123/YOLOv8-main
source venv/bin/activate

# 基础训练命令
yolo detect train data=coco128.yaml model=yolov8n.pt epochs=100 imgsz=640
```

### 方法二：Python API 训练

```python
from ultralytics import YOLO

# 加载模型
model = YOLO("yolov8n.pt")  # 使用预训练模型（推荐）
# 或
model = YOLO("yolov8n.yaml")  # 从头开始训练

# 开始训练
results = model.train(data="coco128.yaml", epochs=100, imgsz=640)
```

---

## 📦 数据集准备

### 1. 数据集格式要求

YOLOv8 支持多种数据集格式，最常用的是 **YOLO 格式**：

```
your_dataset/
├── images/
│   ├── train/
│   │   ├── image1.jpg
│   │   ├── image2.jpg
│   │   └── ...
│   └── val/
│       ├── image101.jpg
│       ├── image102.jpg
│       └── ...
└── labels/
    ├── train/
    │   ├── image1.txt
    │   ├── image2.txt
    │   └── ...
    └── val/
        ├── image101.txt
        ├── image102.txt
        └── ...
```

### 2. 标签文件格式

每个图像对应一个 `.txt` 标签文件，格式为：

```
class_id center_x center_y width height
```

**示例：**
```
0 0.5 0.5 0.3 0.4
1 0.2 0.3 0.1 0.2
```

- `class_id`: 类别ID（从0开始）
- `center_x, center_y`: 边界框中心坐标（归一化到0-1）
- `width, height`: 边界框宽度和高度（归一化到0-1）

### 3. 创建数据集配置文件

创建 `my_dataset.yaml` 文件：

```yaml
# 数据集路径
path: /path/to/your_dataset  # 数据集根目录
train: images/train  # 训练集路径（相对于 path）
val: images/val      # 验证集路径（相对于 path）
test: images/test    # 测试集路径（可选）

# 类别数量
nc: 2  # 类别数量

# 类别名称
names:
  0: cat
  1: dog
```

### 4. 使用现有数据集

YOLOv8 内置支持多种数据集格式：

- **COCO 格式**: 自动识别 `annotations/instances_train.json`
- **VOC 格式**: 自动识别 XML 标注文件
- **YOLO 格式**: 如上所述

---

## 🎯 训练方法

### 命令行训练（CLI）

#### 基础语法

```bash
yolo TASK MODE ARGS
```

- `TASK`: `detect`（检测）、`segment`（分割）、`classify`（分类）
- `MODE`: `train`（训练）、`val`（验证）、`predict`（预测）、`export`（导出）
- `ARGS`: 参数，格式为 `key=value`

#### 训练命令示例

```bash
# 目标检测训练
yolo detect train data=coco128.yaml model=yolov8n.pt epochs=100 imgsz=640

# 图像分割训练
yolo segment train data=coco128-seg.yaml model=yolov8n-seg.pt epochs=100

# 图像分类训练
yolo classify train data=path/to/dataset model=yolov8n-cls.pt epochs=100
```

#### 常用参数组合

```bash
# 完整训练命令示例
yolo detect train \
    data=my_dataset.yaml \
    model=yolov8n.pt \
    epochs=100 \
    imgsz=640 \
    batch=16 \
    device=0 \
    workers=8 \
    project=my_project \
    name=my_experiment \
    patience=50 \
    save=True \
    cache=True
```

### Python API 训练

#### 基础训练

```python
from ultralytics import YOLO

# 加载预训练模型（推荐）
model = YOLO("yolov8n.pt")

# 训练模型
results = model.train(
    data="my_dataset.yaml",
    epochs=100,
    imgsz=640,
    batch=16,
    device=0
)
```

#### 高级训练配置

```python
from ultralytics import YOLO

# 加载模型
model = YOLO("yolov8n.pt")

# 训练模型（完整参数）
results = model.train(
    # 数据配置
    data="my_dataset.yaml",
    
    # 训练参数
    epochs=100,
    imgsz=640,
    batch=16,
    patience=50,  # 早停耐心值
    
    # 设备配置
    device=0,  # GPU ID，或 'cpu'
    workers=8,  # 数据加载线程数
    
    # 优化器配置
    optimizer='SGD',  # 'SGD', 'Adam', 'AdamW', 'RMSProp'
    lr0=0.01,  # 初始学习率
    momentum=0.937,
    weight_decay=0.0005,
    
    # 学习率调度
    cos_lr=False,  # 余弦学习率
    warmup_epochs=3.0,  # 预热轮数
    
    # 数据增强
    hsv_h=0.015,  # 色调增强
    hsv_s=0.7,    # 饱和度增强
    hsv_v=0.4,    # 明度增强
    degrees=0.0,   # 旋转角度
    translate=0.1, # 平移
    scale=0.5,     # 缩放
    flipud=0.0,    # 上下翻转概率
    fliplr=0.5,    # 左右翻转概率
    mosaic=1.0,    # Mosaic 增强概率
    mixup=0.0,     # MixUp 增强概率
    
    # 项目配置
    project="my_project",
    name="my_experiment",
    exist_ok=False,  # 是否覆盖已存在的实验
    
    # 其他
    save=True,      # 保存检查点
    save_period=10, # 每N个epoch保存一次
    cache=False,    # 缓存图像到内存/磁盘
    verbose=True,   # 详细输出
    seed=0,         # 随机种子
    deterministic=True,  # 确定性模式
    plots=True,     # 生成训练图表
)
```

#### 继续训练（从检查点恢复）

```python
from ultralytics import YOLO

# 从检查点恢复训练
model = YOLO("runs/detect/train/weights/last.pt")
results = model.train(resume=True)
```

---

## ⚙️ 训练参数详解

### 核心参数

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `data` | 数据集配置文件路径 | - | `data=coco128.yaml` |
| `model` | 模型文件（.pt 或 .yaml） | - | `model=yolov8n.pt` |
| `epochs` | 训练轮数 | 100 | `epochs=200` |
| `imgsz` | 输入图像尺寸 | 640 | `imgsz=640` 或 `imgsz=416,640` |
| `batch` | 批次大小 | 16 | `batch=32`（-1 为自动批次） |
| `device` | 设备 | - | `device=0` 或 `device=cpu` |
| `workers` | 数据加载线程数 | 8 | `workers=4` |

### 优化器参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `optimizer` | 优化器类型 | `SGD` |
| `lr0` | 初始学习率 | 0.01 |
| `lrf` | 最终学习率（lr0 * lrf） | 0.01 |
| `momentum` | SGD 动量 | 0.937 |
| `weight_decay` | 权重衰减 | 0.0005 |
| `warmup_epochs` | 预热轮数 | 3.0 |
| `warmup_momentum` | 预热动量 | 0.8 |
| `warmup_bias_lr` | 预热偏置学习率 | 0.1 |

### 损失函数参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `box` | 边界框损失权重 | 7.5 |
| `cls` | 分类损失权重 | 0.5 |
| `dfl` | DFL 损失权重 | 1.5 |

### 数据增强参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `hsv_h` | 色调增强范围 | 0.015 |
| `hsv_s` | 饱和度增强范围 | 0.7 |
| `hsv_v` | 明度增强范围 | 0.4 |
| `degrees` | 旋转角度范围 | 0.0 |
| `translate` | 平移范围 | 0.1 |
| `scale` | 缩放范围 | 0.5 |
| `shear` | 剪切范围 | 0.0 |
| `perspective` | 透视变换 | 0.0 |
| `flipud` | 上下翻转概率 | 0.0 |
| `fliplr` | 左右翻转概率 | 0.5 |
| `mosaic` | Mosaic 增强概率 | 1.0 |
| `mixup` | MixUp 增强概率 | 0.0 |
| `copy_paste` | Copy-Paste 增强概率 | 0.0 |

### 其他重要参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `patience` | 早停耐心值 | 50 |
| `save` | 保存检查点 | True |
| `save_period` | 保存周期 | -1 |
| `cache` | 缓存图像 | False |
| `resume` | 从检查点恢复 | False |
| `project` | 项目名称 | - |
| `name` | 实验名称 | - |
| `exist_ok` | 覆盖已存在实验 | False |
| `plots` | 生成图表 | True |
| `seed` | 随机种子 | 0 |
| `deterministic` | 确定性模式 | True |

---

## 📝 训练示例

### 示例 1：快速训练（COCO128 数据集）

```bash
# 命令行
yolo detect train data=coco128.yaml model=yolov8n.pt epochs=3 imgsz=640
```

```python
# Python
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
results = model.train(data="coco128.yaml", epochs=3, imgsz=640)
```

### 示例 2：自定义数据集训练

```python
from ultralytics import YOLO

# 加载预训练模型
model = YOLO("yolov8n.pt")

# 训练自定义数据集
results = model.train(
    data="/path/to/my_dataset.yaml",
    epochs=100,
    imgsz=640,
    batch=16,
    device=0,
    project="my_project",
    name="cat_dog_detection"
)
```

### 示例 3：多 GPU 训练

```bash
# 使用多个 GPU
yolo detect train data=my_dataset.yaml model=yolov8n.pt epochs=100 device=0,1,2,3
```

```python
# Python 多 GPU 训练
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
results = model.train(
    data="my_dataset.yaml",
    epochs=100,
    device=[0, 1, 2, 3]  # 使用 4 个 GPU
)
```

### 示例 4：从检查点恢复训练

```bash
# 命令行恢复训练
yolo detect train resume=True
```

```python
# Python 恢复训练
from ultralytics import YOLO

# 加载检查点
model = YOLO("runs/detect/train/weights/last.pt")
results = model.train(resume=True)
```

### 示例 5：图像分割训练

```bash
# 分割任务训练
yolo segment train data=coco128-seg.yaml model=yolov8n-seg.pt epochs=100
```

```python
# Python 分割训练
from ultralytics import YOLO

model = YOLO("yolov8n-seg.pt")
results = model.train(data="coco128-seg.yaml", epochs=100)
```

### 示例 6：图像分类训练

```bash
# 分类任务训练
yolo classify train data=path/to/dataset model=yolov8n-cls.pt epochs=100
```

```python
# Python 分类训练
from ultralytics import YOLO

model = YOLO("yolov8n-cls.pt")
results = model.train(data="path/to/dataset", epochs=100)
```

---

## 📊 训练输出

### 训练结果位置

训练完成后，结果保存在：

```
runs/detect/train/
├── args.yaml          # 训练参数
├── results.csv        # 训练指标 CSV
├── results.png        # 训练曲线图
├── confusion_matrix.png  # 混淆矩阵
├── F1_curve.png       # F1 曲线
├── PR_curve.png       # PR 曲线
├── weights/
│   ├── best.pt        # 最佳模型权重
│   └── last.pt        # 最后一个 epoch 的权重
└── ...
```

### 训练指标

训练过程中会显示以下指标：

- **损失值**:
  - `train/box_loss`: 边界框损失
  - `train/cls_loss`: 分类损失
  - `train/dfl_loss`: DFL 损失

- **验证指标**:
  - `metrics/precision(B)`: 精度
  - `metrics/recall(B)`: 召回率
  - `metrics/mAP50(B)`: mAP@0.5
  - `metrics/mAP50-95(B)`: mAP@0.5:0.95

### 查看训练结果

```python
from ultralytics import YOLO

# 加载训练好的模型
model = YOLO("runs/detect/train/weights/best.pt")

# 查看模型信息
model.info()

# 验证模型
results = model.val()
```

---

## ❓ 常见问题

### 1. 内存不足（OOM）

**解决方案：**
- 减小批次大小：`batch=8` 或 `batch=4`
- 减小图像尺寸：`imgsz=416`
- 使用自动批次：`batch=-1`
- 关闭缓存：`cache=False`

```bash
yolo detect train data=my_dataset.yaml model=yolov8n.pt batch=8 imgsz=416
```

### 2. 训练速度慢

**解决方案：**
- 增加数据加载线程：`workers=16`
- 启用缓存：`cache=True` 或 `cache=ram`
- 使用 GPU：`device=0`
- 减小图像尺寸：`imgsz=416`

```bash
yolo detect train data=my_dataset.yaml model=yolov8n.pt workers=16 cache=True device=0
```

### 3. 过拟合

**解决方案：**
- 增加数据增强
- 使用 Dropout（分类任务）
- 增加权重衰减：`weight_decay=0.0005`
- 使用早停：`patience=20`

```python
results = model.train(
    data="my_dataset.yaml",
    epochs=100,
    weight_decay=0.001,
    patience=20,
    # 增强数据增强
    mosaic=1.0,
    mixup=0.1,
    degrees=10.0
)
```

### 4. 训练不收敛

**解决方案：**
- 检查学习率：尝试 `lr0=0.001` 或 `lr0=0.1`
- 使用预训练模型：`model=yolov8n.pt`
- 检查数据标注是否正确
- 增加训练轮数

### 5. 如何选择模型大小？

| 模型 | 参数量 | 速度 | 精度 | 适用场景 |
|------|--------|------|------|----------|
| yolov8n | 最小 | 最快 | 较低 | 移动端、实时检测 |
| yolov8s | 小 | 快 | 中等 | 平衡速度和精度 |
| yolov8m | 中 | 中等 | 较高 | 通用场景 |
| yolov8l | 大 | 较慢 | 高 | 高精度需求 |
| yolov8x | 最大 | 最慢 | 最高 | 研究、竞赛 |

### 6. 如何监控训练？

**使用 TensorBoard：**

```bash
# 安装 TensorBoard（已包含在 requirements.txt）
tensorboard --logdir runs/detect/train
```

然后在浏览器打开 `http://localhost:6006`

---

## 💡 训练技巧

1. **使用预训练模型**：从 `yolov8n.pt` 开始，而不是从 `.yaml` 开始
2. **数据增强**：合理使用 Mosaic、MixUp 等增强
3. **学习率调整**：如果训练不稳定，降低学习率
4. **早停机制**：使用 `patience` 参数避免过拟合
5. **验证集**：确保验证集足够大且具有代表性
6. **批次大小**：根据 GPU 显存调整，通常 16-32
7. **图像尺寸**：640 是标准尺寸，可根据需求调整

---

## 📚 更多资源

- [YOLOv8 官方文档](https://docs.ultralytics.com)
- [GitHub 仓库](https://github.com/ultralytics/ultralytics)
- [示例教程](examples/tutorial.ipynb)
- [项目结构分析](项目结构分析.md)

---

## 🎯 快速参考

### 最常用的训练命令

```bash
# 基础训练
yolo detect train data=my_dataset.yaml model=yolov8n.pt epochs=100

# 完整训练（推荐）
yolo detect train \
    data=my_dataset.yaml \
    model=yolov8n.pt \
    epochs=100 \
    imgsz=640 \
    batch=16 \
    device=0 \
    project=my_project \
    name=experiment1
```

### Python 快速训练模板

```python
from ultralytics import YOLO

# 加载模型
model = YOLO("yolov8n.pt")

# 训练
results = model.train(
    data="my_dataset.yaml",
    epochs=100,
    imgsz=640,
    batch=16,
    device=0
)

# 验证
results = model.val()

# 预测
results = model.predict("path/to/image.jpg")
```

---

**祝您训练顺利！** 🚀

